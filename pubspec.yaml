name: subtree
description: A state manager for those who like BLoC but don't like BLoC verbosity.
version: 0.5.0

homepage: https://github.com/vkubiv/subtree
repository: https://github.com/vkubiv/subtree
issue_tracker: https://github.com/vkubiv/subtree/issues


environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mocktail: ^0.3.0

flutter:

